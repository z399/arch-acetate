**All of the install scripts found in the internet for arch linux are super bloated, So I decided to create my own, A minimal "lazy" script**

# Installation


```
chmod +x basebuild && ./basebuild
(make the bash executable and run)

